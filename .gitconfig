[alias]
    # list aliases
    la = "!git config -l | grep alias | cut -c 7-"

    # one-line log
    l = log --pretty=format:"%C(yellow)%h\\ %ad%Cred%d\\ %Creset%s%Cblue\\ [%cn]" --decorate --date=short

    # Add newly created remote to existing project
    new = remote add origin
[core]
	autocrlf = false
[user]
	name = Kreger51
	email = selim.belhaouane@gmail.com
[push]
	default = matching
