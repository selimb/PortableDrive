#!/bin/bash
homesize=${#HOME}
if [[ $homesize -gt 3 ]]; then
    echo HOME=$HOME - size=$homesize
    echo Set HOME to drive root first.
    exit
fi
cd ~/Apps
makelist
echo List made in "~/Apps"
cat LIST
echo
cd PortableApps
makelist
echo List made in "~/Apps/PortableApps"
cat LIST